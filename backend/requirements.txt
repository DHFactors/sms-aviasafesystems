# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-dotenv>=1.0.0

# Database (PostgreSQL / SQLAlchemy)
sqlalchemy[asyncio]>=2.0.29
asyncpg>=0.29.0
alembic>=1.13.1

# Firebase Admin SDK (Auth-only since B4; Firestore removed)
# firebase-admin still pulls google-cloud-firestore transitively for Auth,
# but the app no longer uses Firestore directly. Direct dependency removed
# in B5 — kept only via firebase-admin.
firebase-admin>=6.6.0
# google-cloud-firestore>=2.11.0  # deprecated B5 — Firestore removed

# Authentication
python-multipart>=0.0.6

# AI Integration
google-generativeai>=0.5.0

# Security & Validation
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Validation
email-validator>=2.1.0

# Rate Limiting (Redis-backed)
redis[hiredis]>=5.0.7

# AI Chat Assistant (Groq — Safety & Compliance Copilot)
groq>=0.11.0
httpx>=0.27.0

# Utilities
loguru>=0.7.2
python-dateutil>=2.8.2

# PDF report generation
reportlab>=4.1.0
jinja2>=3.1.4

# Background Scheduling
apscheduler>=3.10.4

# Retry & Resilience
tenacity>=8.2.3

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.5
pytest-mock>=3.12.0
