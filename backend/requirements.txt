# Core Framework
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-dotenv>=1.0.0

# Firebase Admin SDK
# NOTE: firebase-admin>=6.6.0 required for named Firestore databases
# (firestore.client(database_id=...) support added in 6.6.0).
firebase-admin>=6.6.0
google-cloud-firestore>=2.11.0

# Authentication
python-multipart>=0.0.6

# AI Integration
google-generativeai>=0.5.0

# Security & Validation
pydantic>=2.6.0
pydantic-settings>=2.2.0

# Validation
email-validator>=2.1.0

# Rate Limiting (Redis-backed)
redis[hiredis]>=5.0.7

# AI Chat Assistant (Groq — Safety & Compliance Copilot)
groq>=0.11.0
httpx>=0.27.0

# Utilities
loguru>=0.7.2
python-dateutil>=2.8.2

# PDF report generation
reportlab>=4.1.0
jinja2>=3.1.4

# Background Scheduling
apscheduler>=3.10.4

# Retry & Resilience
tenacity>=8.2.3

# Testing
pytest>=8.1.0
pytest-asyncio>=0.23.5
